name: Automated Tests

on:
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM UTC
  workflow_dispatch:
    inputs:
      test_type:
        description: 'Type of test to run'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - unit
          - integration

env:
  PYTHON_VERSION: '3.11'

jobs:
  # ==========================================
  # Scheduled Unit Tests
  # ==========================================
  unit-tests:
    name: 🕐 Scheduled Tests
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
    
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
    
    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        echo "Installing dependencies with fallbacks..."
        if pip install -r requirements-ci.txt; then
          echo "✅ CI dependencies installed"
        elif pip install -r requirements.txt; then
          echo "✅ Full dependencies installed"
        else
          echo "⚠️ Installing minimal dependencies"
          pip install pandas numpy transformers datasets torch librosa soundfile jiwer openpyxl || echo "⚠️ Some dependencies failed"
        fi
    
    - name: 📁 Create Test Environment
      run: |
        mkdir -p data results models tests
        python tests/create_test_data.py || echo "Using basic setup"
    
    - name: 🧪 Run Scheduled Tests
      run: |
        echo "🧪 Running scheduled tests..."
        export PYTHONPATH="$(pwd)/src:$(pwd):$PYTHONPATH"
        
        echo "🔍 Debug: Current directory and Python path"
        pwd
        echo "PYTHONPATH: $PYTHONPATH"
        ls -la src/
        echo "Testing basic Python imports..."
        python -c "import pandas, numpy, re, unicodedata, os, json; print('✅ Basic dependencies available')" || echo "❌ Basic dependencies missing"
        
        echo "🔍 Testing core modules..."
        modules_tested=0
        modules_passed=0
        
        echo "Testing audio_processing module..."
        if python -c "import sys; sys.path.insert(0, 'src'); from audio_processing import DatasetAudioProcessor; print('✅ Audio processing: PASS')"; then
          modules_passed=$((modules_passed + 1))
        else
          echo "❌ Audio processing: FAIL"
        fi
        modules_tested=$((modules_tested + 1))
        
        echo "Testing disfluency_detector module..."
        echo "Debug: Testing basic module import first..."
        if python -c "import sys; sys.path.insert(0, 'src'); import disfluency_detector; print('Module imported successfully')"; then
          echo "✅ Module imports successfully"
          if python -c "import sys; sys.path.insert(0, 'src'); from disfluency_detector import HindiDisfluencyDetector; print('✅ Disfluency detector: PASS')"; then
            modules_passed=$((modules_passed + 1))
          else
            echo "❌ Disfluency detector class import: FAIL"
            echo "Debug: Attempting to list available classes in disfluency_detector"
            python -c "import sys; sys.path.insert(0, 'src'); import disfluency_detector; print('Available in module:', [x for x in dir(disfluency_detector) if not x.startswith('_')])" || echo "Module listing failed"
          fi
        else
          echo "❌ Disfluency detector module: FAIL"
          echo "Debug: Checking if src directory contains the file..."
          ls -la src/disfluency_detector.py || echo "File not found"
        fi
        modules_tested=$((modules_tested + 1))
        
        echo "Testing model_evaluation module..."
        if python -c "import sys; sys.path.insert(0, 'src'); from model_evaluation import ModelManager; print('✅ Model evaluation: PASS')"; then
          modules_passed=$((modules_passed + 1))
        else
          echo "❌ Model evaluation: FAIL"
        fi
        modules_tested=$((modules_tested + 1))
        
        echo "📊 Results: $modules_passed/$modules_tested modules passed"
        if [ $modules_passed -eq $modules_tested ]; then
          echo "✅ All tests passed!"
          exit 0
        else
          echo "❌ Some tests failed"
          exit 1
        fi

  # ==========================================
  # Performance Check
  # ==========================================
  performance:
    name: 📈 Performance Check
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
    
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
    
    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install psutil
        echo "Installing dependencies with fallbacks..."
        if pip install -r requirements-ci.txt; then
          echo "✅ CI dependencies installed"
        elif pip install -r requirements.txt; then
          echo "✅ Full dependencies installed"
        else
          echo "⚠️ Installing minimal dependencies"
          pip install pandas numpy transformers datasets torch librosa soundfile jiwer openpyxl || echo "⚠️ Some dependencies failed"
        fi
    
    - name: 📊 Performance Check
      run: |
        echo "📊 Running performance checks..."
        export PYTHONPATH="$(pwd)/src:$PYTHONPATH"
        
        echo "💾 System Resources:"
        python -c "import psutil; print(f'CPU: {psutil.cpu_percent(interval=1):.1f}%'); print(f'Memory: {psutil.virtual_memory().percent:.1f}%')" || echo "⚠️ psutil not available"
        
        echo "⏱️ Testing ModelManager load time..."
        python -c "import time; start = time.time(); from model_evaluation import ModelManager; load_time = time.time() - start; print(f'⏱️ ModelManager load: {load_time:.3f}s'); print('✅ Performance OK' if load_time < 5.0 else '⚠️ Slow performance')" || echo "❌ Performance test failed"

  # ==========================================
  # Health Summary
  # ==========================================
  health-summary:
    name: 📋 Health Summary
    runs-on: ubuntu-latest
    needs: [unit-tests, performance]
    if: always()
    
    steps:
    - name: 📋 Generate Report
      run: |
        echo "📋 Josh Talks Health Report"
        echo "=========================="
        echo "🕐 Date: $(date)"
        echo "🧪 Unit Tests: ${{ needs.unit-tests.result }}"
        echo "📈 Performance: ${{ needs.performance.result }}"
        echo "=========================="
        
        if [[ "${{ needs.unit-tests.result }}" == "success" && "${{ needs.performance.result }}" == "success" ]]; then
          echo "✅ All health checks passed!"
        else
          echo "⚠️ Some health checks failed"
        fi
